import { StyleSheet } from "react-native";

export const InputStyles = StyleSheet.create({
  searchInput: {
    borderWidth: 1,
    borderColor: "#333",
    backgroundColor: "#1E1E1E",
    borderRadius: 12,
    padding: 16,
    fontSize: 16,
    color: "#fff",
    marginHorizontal: 16,
    marginBottom: 16,
  },
});